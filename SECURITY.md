# 安全策略

AgentMesh 是一个**控制面**：它持有到各台主机的访问凭据，并能在那些主机上执行命令。
因此它的威胁模型和普通库不一样，请先读第 2 节再部署。

---

## 1. 支持的版本

| 版本 | 是否接受安全修复 |
|---|---|
| `0.1.x` | ✅ |
| `main` 分支 | ✅ |

项目处于 0.1 阶段，只有最新版本会被修。没有 LTS 分支。

---

## 2. 部署前必读：控制台没有认证

**`mesh serve` 提供的 Web 控制台没有任何身份认证。** 这不是"默认关闭、需要开启"的功能——它**不存在**。

请求处理是一张直接的路由表，没有登录、没有 token、没有来源校验。任何能连上该端口的人都可以：

- 列出、添加、修改、删除节点（包括写入新的 SSH 主机与凭据）；
- 向任意节点派发任务，**任务会在远端主机上以你的身份执行命令**；
- 批准或拒绝审批请求；
- 读取任务历史与事件流。

默认绑定是 `127.0.0.1:7331`，也就是说**只有本机能连**。这个默认值是安全的，请保持它。

### 不要这样做

```bash
mesh serve --host 0.0.0.0     # ❌ 等于把无认证的远程命令执行入口挂到网上
```

放在公网 VPS 上、或绑到局域网网卡上，都等同于把上表所有能力公开。

### 需要远程访问就这样做

用 SSH 端口转发，把控制台留在回环地址上：

```bash
ssh -N -L 7331:127.0.0.1:7331 user@your-host
# 然后在本地浏览器打开 http://127.0.0.1:7331
```

这样访问控制复用 SSH 的认证，不需要给控制台另加一套。

> 如果你确实需要一个可对外提供服务的控制台，请先开 issue 讨论——这是**尚未实现**的能力，
> 不是配置问题。上游项目也不应假设它存在。

---

## 3. 凭据是怎么处理的

理解这一节能帮你判断哪些信息值得保护。

| 凭据 | 存在哪 | 保护方式 |
|---|---|---|
| SSH 密码 | `~/.agentmesh/secrets.env`（mode `0600`，**明文**） | **文件权限**，不是加密 |
| SSH 私钥 | 你的 `~/.ssh/`，由 `ssh` 自己读 | 复用 OpenSSH 的机制 |
| A2A bearer token | 同上（`secrets.env`）或节点环境变量 | 同 SSH 密码 |
| 模型 API key | **控制面不持有** | 各节点自带 |
| 节点凭据的**值** | 从不写入 `nodes.json` | 节点里只存**变量名**（`passwordEnv`） |

几个明确的设计事实：

- **`nodes.json` 里没有密码。** 节点记录只保存"密码在哪个环境变量里"这个名字，值从环境读取。
  所以注册表可以安全地备份、贴进 issue 讨论。
- **界面拿不到凭据。** 节点 API 返回前会剥掉 `token` / `password` / `sshPassword` 字段，浏览器端只收到布尔标记（如 `hasToken`）。
- **`mesh secrets` 是明文文件。** 这是有意的取舍：不加密，靠 `0600` 权限保护。
  Windows 上 `chmod` 对权限模型无效，保护实际来自你用户目录的 ACL。
  **更优解是改用 SSH 密钥认证**，文档按这个顺序推荐。
- **ACP 客户端默认不声明 `fs` / `terminal` 能力**，agent 会改用自身工具在主机上操作；
  审批策略默认 `deny`。
- **A2A 节点没配 token 就不启用**，不会静默地发未鉴权请求。

### 你仍然要自己守住的部分

- `secrets.env`、SSH 私钥、`known_hosts` **都不该提交到 git**。本仓库的 `.gitignore` 已覆盖这些路径；
  如果你的部署仓库是另一个，请自行确认。
- 控制面**能**在远端主机上执行命令。授予它访问权的主机，等于授予它一份"以你的身份干活"的能力。
  只注册你确实需要的节点。

---

## 4. 报告漏洞

请**不要**开公开 issue。

用 GitHub 的私密报告通道：

> **Security** 标签页 → **Report a vulnerability**
> <https://github.com/yanyu-1/agentmesh/security/advisories/new>

如果该入口不可用，就开一个**不含任何细节**的 issue，只说"我需要一个私密渠道报告安全问题"，
维护者会来联系你。

报告里请包含：

- 受影响的版本或 commit；
- 复现步骤，最好附最小化的命令/请求；
- 实际影响（能读到什么、能改到什么、是否需要本地访问）；
- 如果有，附上你建议的修复方向。

**我们会在 7 天内回第一次**，并在修复发布后致谢（除非你希望匿名）。

⚠️ 贴日志或截图前请先脱敏：主机地址、用户名、密码、token、私钥。

---

## 5. 我们认为在范围内

- 绕过硬编码的凭据隔离（例如让密码经 API 回显、或落进 `nodes.json`）；
- 在 Web 控制台里注入（XSS / 模板注入），尤其是能让任务内容或节点名变成可执行内容；
- 审批被绕过：未经操作员明确同意就完成任务；
- Web 文件读写接口的路径穿越（编辑器类功能）；
- 命令注入：节点名、工作目录、SSH 选项等字段被拼进 shell/ssh 命令行执行；
- SSH/ASKPASS 包装器把密码泄漏给非目标进程，或泄漏进进程列表 / 磁盘。

## 6. 我们**不**认为在范围内

- **你主动 `mesh serve --host 0.0.0.0`（或绑到非回环地址）导致控制台暴露**——见第 2 节，这是已知且已文档化的设计边界；
- 被管智能体自身（Hermes、opencode、Claude Code 等）内部的漏洞——请报告给对应上游；
- `secrets.env` 是明文这件事本身（已知取舍，见第 3 节）；
- 需要攻击者已经拥有你本机账户或已能读 `~/.agentmesh/` 的攻击场景；
- 依赖项漏洞——本项目零运行时依赖。

---

## 7. 加固清单

部署时逐条确认：

- [ ] `mesh serve` 保持默认绑定（`127.0.0.1`），远程访问走 SSH 端口转发；
- [ ] `~/.agentmesh/` 权限正确（POSIX 下 `0700`，`secrets.env` 为 `0600`）；
- [ ] 优先用 SSH 密钥而非密码；
- [ ] 只注册确实需要控制的节点；
- [ ] 确认目标主机上的 `known_hosts` 已固定，且没有关掉 `StrictHostKeyChecking`；
- [ ] 审批策略保持 `deny`/`ask`，不要为了省事改成自动放行；
- [ ] 定期检查 `~/.agentmesh/nodes.json`，删掉不再使用的节点（每个节点都是一条持久的访问路径）。
